import {
  BellRing, Eye, Headphones, Link2, Send, SlidersHorizontal, Sparkles,
  UserCheck, UsersRound, Vote,
} from "lucide-react";
import type { LucideIcon } from "lucide-react";

export const FEATURE_ICONS: Record<string, LucideIcon> = {
  BellRing,
  Eye,
  Headphones,
  Link2,
  Send,
  SlidersHorizontal,
  Sparkles,
  UserCheck,
  UsersRound,
  Vote,
};

export const FEATURE_ICON_NAMES = Object.keys(FEATURE_ICONS);
